# Optional. The app falls back to the values baked into src/lib/supabase.ts,
# so it works on Vercel without any dashboard configuration.
# Set these to point the app at a different Supabase project.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
